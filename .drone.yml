kind: pipeline
name: default

steps:
- name: run
  image: rust
  # volumes:
  # - name: vendor
  #   path: /drone/src/vendor/
  commands:
  # - cargo vendor
  - cargo build --release
  - cargo run --release

- name: deploy
  image: appleboy/drone-scp
  settings:
    hosts: sh.thepog.cn
    username: root
    password: 330681Ycx
    target: /bin
    source: target/release/hello-ex
# volumes:
#   - name: vendor
#     host:
#       path: /root/vendor/